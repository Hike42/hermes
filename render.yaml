services:
  - type: web
    name: youtube-downloader
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # Note: Render nécessite un Dockerfile pour installer yt-dlp et ffmpeg
